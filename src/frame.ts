import {Roll} from "../src/Roll";

export class Frame {

    // nextFrame:Frame = undefined;
    private rolls: Roll[] = [];

    public score(): number {
        return 0;
    }

    public roll(pins: number): number {
        return 0;
    }

}
